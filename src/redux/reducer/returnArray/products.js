const initial = [];

const reducer = (state = initial, action) => {
    const { type, data } = action;
    switch (type) {
        // case value:

        //      break;

        default:
            return state;
    }
};

export default reducer